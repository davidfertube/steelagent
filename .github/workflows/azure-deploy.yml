name: Deploy to Azure

on:
  push:
    branches: [main, mystifying-keller]
  pull_request:
    branches: [main, mystifying-keller]
  workflow_dispatch:

env:
  AZURE_FUNCTIONAPP_NAME: steel-agent-api
  AZURE_FUNCTIONAPP_PACKAGE_PATH: backend
  PYTHON_VERSION: '3.11'
  NODE_VERSION: '20'

jobs:
  # Build and test frontend
  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run tests
        run: npm test -- --run

      - name: Build application
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: https://steel-agent-api.azurewebsites.net

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: out
          retention-days: 1

  # Build and test backend
  build-backend:
    name: Build Backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-asyncio httpx

      - name: Run tests
        # Tests run in demo mode (no API keys) to avoid external dependencies
        run: pytest backend/tests/ -v --tb=short

      - name: Prepare deployment package
        run: |
          cd backend
          pip install --target=".python_packages/lib/site-packages" -r ../requirements.txt

      - name: Upload backend artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: backend
          retention-days: 1

  # Deploy frontend to Azure Static Web Apps
  deploy-frontend:
    name: Deploy Frontend to Azure SWA
    runs-on: ubuntu-latest
    needs: [build-frontend, build-backend]
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download frontend build
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: out

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_RED_FLOWER_0152EE60F }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/out"
          api_location: ""
          output_location: ""
          skip_app_build: true

  # Deploy backend to Azure Functions
  # Note: Requires AZURE_FUNCTIONAPP_PUBLISH_PROFILE secret
  # To get this: Azure Portal > Function App > Overview > Get publish profile
  deploy-backend:
    name: Deploy Backend to Azure Functions
    runs-on: ubuntu-latest
    needs: [build-frontend, build-backend]
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && vars.SKIP_BACKEND_DEPLOY != 'true'

    steps:
      - name: Download backend build
        uses: actions/download-artifact@v4
        with:
          name: backend-build
          path: backend

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Deploy to Azure Functions
        uses: Azure/functions-action@v1
        with:
          app-name: ${{ env.AZURE_FUNCTIONAPP_NAME }}
          package: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
          publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}
          scm-do-build-during-deployment: true
          enable-oryx-build: true

  # Cleanup on PR close
  close-pr:
    name: Close PR Preview
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    steps:
      - name: Close Pull Request
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_RED_FLOWER_0152EE60F }}
          action: "close"
